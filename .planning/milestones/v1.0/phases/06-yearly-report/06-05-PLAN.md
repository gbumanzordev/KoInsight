---
phase: 06-yearly-report
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - apps/server/src/reports/reports-router.ts
  - apps/server/src/reports/__tests__/reports-router.test.ts
  - apps/server/src/app.ts
autonomous: true
requirements: [REPORT-01, REPORT-03]
tags: [phase-6, router, zod, supertest]

must_haves:
  truths:
    - "GET /api/reports/years returns 200 with { years: number[] } sorted descending"
    - "GET /api/reports/yearly?year=2024 returns 200 with the documented YearlyReport JSON shape"
    - "GET /api/reports/yearly without ?year returns 400 with the Zod flattened error payload"
    - "GET /api/reports/yearly?year=abc returns 400"
    - "GET /api/reports/yearly?year=1899 returns 400 (range check 1900..2200)"
    - "Repository / service errors yield 500 with a generic body and a console.error log; full error never reaches the client"
    - "/api/reports/* is mounted in apps/server/src/app.ts BEFORE the express.static and SPA catch-all"
  artifacts:
    - path: "apps/server/src/reports/reports-router.ts"
      provides: "Express router with GET /years and GET /yearly routes"
      exports: ["reportsRouter"]
    - path: "apps/server/src/reports/__tests__/reports-router.test.ts"
      provides: "supertest end-to-end coverage of 200/400/500 paths plus JSON shape (REPORT-01, REPORT-03, REPORT-UI-04 coverage block presence)"
      contains: "supertest"
    - path: "apps/server/src/app.ts"
      provides: "Mount point app.use('/api/reports', reportsRouter) before SPA catch-all"
      contains: "/api/reports"
  key_links:
    - from: "apps/server/src/reports/reports-router.ts"
      to: "apps/server/src/reports/reports-service.ts"
      via: "ReportsService.getYears / ReportsService.getYearly"
      pattern: "ReportsService"
    - from: "apps/server/src/app.ts"
      to: "apps/server/src/reports/reports-router.ts"
      via: "app.use mount"
      pattern: "app.use\\('/api/reports'"
---

<objective>
Expose the Phase 6 service over HTTP. A thin `reports-router.ts` validates `?year=YYYY` with Zod, delegates to `ReportsService`, and ships JSON. End-to-end supertest coverage locks the wire contract (status codes, JSON shape, error payload).

Purpose: REPORT-01 (`/api/reports/yearly`), REPORT-03 (`/api/reports/years`).
Output: router module + supertest suite + one mount line in `app.ts`.
</objective>

<execution_context>
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-yearly-report/06-CONTEXT.md
@.planning/phases/06-yearly-report/06-RESEARCH.md
@.planning/phases/06-yearly-report/06-PATTERNS.md
@apps/server/src/enrichment/router.ts
@apps/server/src/stats/stats-router.ts
@apps/server/src/stats/stats-router.test.ts
@apps/server/src/enrichment/__tests__/unmatched-router.test.ts
@apps/server/src/app.ts

<interfaces>
Router (per RESEARCH Pattern 1):

```typescript
import { Router, Request, Response } from 'express';
import { z } from 'zod';
import { ReportsService } from './reports-service';

const router = Router();

const yearlyQuery = z.object({
  year: z.coerce.number().int().min(1900).max(2200),
});

router.get('/years', async (_req, res) => { /* try/catch -> 200 / 500 */ });
router.get('/yearly', async (req, res) => { /* safeParse -> 400 / 200 / 500 */ });

export { router as reportsRouter };
```

Mount in `apps/server/src/app.ts` next to `app.use('/api/enrichment', enrichmentRouter);` (around line 40), strictly BEFORE `app.use(express.static(...))` and the `app.get(/.*/, ...)` SPA fallback (lines 43-46).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-05-01: Add supertest router tests covering REPORT-01 + REPORT-03 (RED)</name>
  <files>apps/server/src/reports/__tests__/reports-router.test.ts</files>
  <behavior>
    - Build a fresh Express app per test: `const app = express(); app.use(express.json()); app.use('/reports', reportsRouter);`
    - Seed via the fixture from 06-03 to land a few books in 2024 (one fully read, one 50% read, one with NULL nationality, one with two genres).
    - Assertions:
      - `GET /reports/years` -> 200; body is `{ years: [2024, ...] }`.
      - `GET /reports/yearly?year=2024` -> 200; body is a `YearlyReport`. Validate with `expect(body).toMatchObject({ year: 2024, totals: { books: expect.any(Number), pages: expect.any(Number), readTimeSeconds: expect.any(Number) }, genre: expect.any(Array), nationality: expect.any(Array), decade: expect.any(Array), language: expect.any(Array), coverage: { total_books: expect.any(Number), genre_known: expect.any(Number), nationality_known: expect.any(Number), publication_year_known: expect.any(Number), original_language_known: expect.any(Number) } })`.
      - REPORT-UI-04 coverage assertion: `body.coverage.total_books >= body.coverage.genre_known` (denominator is books-with-any-genre, not the bar-height sum).
      - Top-N + Unknown sanity: in a year with > 11 distinct nationalities, `body.nationality` includes a `{ key: 'Other', count: ... }` entry AND a `{ key: 'Unknown', count: ... }` entry when applicable.
      - Decade sequence is monotonically increasing in `key` (with trailing 'Unknown' if present).
      - `GET /reports/yearly` (no query) -> 400; body has the Zod flattened shape `{ error: { fieldErrors: { year: [...] }, ... } }`.
      - `GET /reports/yearly?year=abc` -> 400.
      - `GET /reports/yearly?year=1899` -> 400.
      - Error path: stub `ReportsService.getYearly` to throw; assert 500 + generic body `{ error: 'Failed to load yearly report' }`. (Use `vi.spyOn` on the imported service.)
    - Empty-year contract: a year with no `page_stat` rows still returns 200 with valid JSON shape (Nyquist 6).
  </behavior>
  <action>
    Mirror `apps/server/src/stats/stats-router.test.ts` structure (lines 1-42) and `apps/server/src/enrichment/__tests__/unmatched-router.test.ts` (lines 1-50) for the seeding pattern. Reuse `seedYearlyReportScenario` from 06-03.

    Create the test file. Reference the not-yet-implemented router via `import { reportsRouter } from '../reports-router'`, this fails at import time, which is the RED step. Commit `test(06): add reports-router supertest suite (red)`.
  </action>
  <verify>
    <automated>npm --workspace=server exec vitest run apps/server/src/reports/__tests__/reports-router.test.ts</automated>
  </verify>
  <done>Test file fails on import (router not yet implemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-05-02: Implement reports-router and mount on /api/reports (GREEN)</name>
  <files>apps/server/src/reports/reports-router.ts, apps/server/src/app.ts</files>
  <behavior>
    - All router tests from 06-05-01 pass.
    - 200 / 400 / 500 paths covered.
    - `app.ts` mounts the router exactly once at `/api/reports`, BEFORE `express.static` and the SPA catch-all.
    - Plugin (`plugins/koinsight.koplugin/call_api.lua`) is NOT modified, Phase 6 endpoints are frontend-only and the Lua plugin does not consume them.
  </behavior>
  <action>
    Write `reports-router.ts` per the `<interfaces>` block, mirroring `apps/server/src/enrichment/router.ts` lines 18-48 for the `safeParse` -> 400 pattern and the try/catch -> 500 pattern. Generic 500 body: `{ error: 'Failed to load yearly report' }` for the yearly route, `{ error: 'Failed to load years' }` for the years route. `console.error(error)` on every catch.

    Edit `apps/server/src/app.ts`:
    - Add `import { reportsRouter } from './reports/reports-router';` near the existing router imports.
    - Add `app.use('/api/reports', reportsRouter);` immediately after `app.use('/api/enrichment', enrichmentRouter);` (see line 40 in the existing file). Confirm the mount sits BEFORE the static + SPA catch-all (lines 43-46 today).

    Run prettier, run tests. Atomic commit `feat(06): add reports router and mount on /api/reports`.
  </action>
  <verify>
    <automated>npm --workspace=server exec vitest run apps/server/src/reports/__tests__/reports-router.test.ts &amp;&amp; npm --workspace=server test</automated>
  </verify>
  <done>All router tests green; full server suite green; route mounted in app.ts before catch-all.</done>
</task>

</tasks>

<threat_model>
- T-06-05-01 (Tampering / SQL injection via `?year=`): Zod `z.coerce.number().int().min(1900).max(2200)` validates and bounds the input before it reaches SQL. Mitigate.
- T-06-05-02 (Information disclosure on error): Generic 500 body; full error to `console.error` only. Mitigate.
- T-06-05-03 (DoS via crafted year): Bounded range 1900..2200 + indexed integer scan. Accept.
- T-06-05-04 (CORS): App already has `cors({ origin: '*' })`; not changed by this plan. Existing posture inherited; no new mitigation.
</threat_model>

<verification>
- `npm --workspace=server test` (full suite) green.
- Manual: `curl 'http://localhost:3000/api/reports/years'` returns `{ years: [...] }`; `curl 'http://localhost:3000/api/reports/yearly?year=2024'` returns the documented JSON.
</verification>

<success_criteria>
- REPORT-01: GET /api/reports/yearly returns documented JSON shape.
- REPORT-03: GET /api/reports/years returns sorted-desc years.
- Router mounted before SPA catch-all (no shadowing).
</success_criteria>

<output>
After completion, create `.planning/phases/06-yearly-report/06-05-SUMMARY.md`.
</output>
