---
phase: 05-manual-edit-unmatched-inbox
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/web/package.json
  - apps/web/src/api/books.ts
  - apps/web/src/api/use-book-with-data.ts
  - apps/web/src/components/provenance-badge/provenance-badge.tsx
  - apps/web/src/components/provenance-badge/provenance-badge.module.css
  - apps/web/src/components/re-enrich-button/re-enrich-button.tsx
  - apps/web/src/pages/book-page/book-metadata-edit-modal.tsx
  - apps/web/src/pages/book-page/book-metadata-form.tsx
  - apps/web/src/pages/book-page/author-row-editor.tsx
  - apps/web/src/pages/book-page/book-page.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-05]
tags: [phase-5, web-ui, edit-form, provenance-badge, re-enrich]

must_haves:
  truths:
    - "User clicks 'Edit metadata' on book detail, a Mantine Modal (size='lg') opens with a @mantine/form form pre-filled from the current book"
    - "Each editable field (Authors, Genres, Publication year, Original language) renders a ProvenanceBadge next to its label: 'manual' (yellow), 'OpenLibrary' (blue), or nothing when *_source is null"
    - "Authors are edited via row-per-author editor: name TextInput + nationality Select + read-only OL key + unlink ActionIcon + remove + move-up/down ActionIcons"
    - "Save submits PATCH /api/books/:bookId/metadata; success toast fires; SWR mutates the book key; modal closes; cancel discards state silently"
    - "Book detail page shows a Re-enrich button that POSTs to /re-enrich and polls via SWR refreshInterval:2000 while enrichment_status in {pending,running}"
  artifacts:
    - path: "apps/web/src/components/provenance-badge/provenance-badge.tsx"
      provides: "ProvenanceBadge pure presentational component"
      exports: ["ProvenanceBadge"]
    - path: "apps/web/src/components/re-enrich-button/re-enrich-button.tsx"
      provides: "ReEnrichButton (primary + row variants)"
      exports: ["ReEnrichButton"]
    - path: "apps/web/src/pages/book-page/book-metadata-edit-modal.tsx"
      provides: "BookMetadataEditModal (Modal + Form composition)"
      exports: ["BookMetadataEditModal"]
    - path: "apps/web/src/pages/book-page/book-metadata-form.tsx"
      provides: "BookMetadataForm (useForm + zod4Resolver + all inputs)"
      exports: ["BookMetadataForm"]
    - path: "apps/web/src/pages/book-page/author-row-editor.tsx"
      provides: "AuthorRowEditor (single row with name/nationality/OL-key/reorder/remove)"
      exports: ["AuthorRowEditor"]
    - path: "apps/web/src/api/books.ts"
      provides: "patchBookMetadata + reEnrichBook API wrappers"
      exports: ["patchBookMetadata", "reEnrichBook"]
  key_links:
    - from: "book-metadata-form.tsx"
      to: "@koinsight/common metadataPatchSchema"
      via: "import { metadataPatchSchema, MetadataPatch } from '@koinsight/common/types'"
      pattern: "zod4Resolver\\(metadataPatchSchema\\)"
    - from: "book-page.tsx"
      to: "book-metadata-edit-modal.tsx + re-enrich-button.tsx"
      via: "Edit metadata button opens Modal via useDisclosure; Re-enrich button in header"
      pattern: "BookMetadataEditModal|ReEnrichButton"
    - from: "use-book-with-data.ts"
      to: "book.enrichment_status (server)"
      via: "SWR refreshInterval: (latest) => isOpenStatus(latest?.enrichment_status) ? 2000 : 0"
      pattern: "refreshInterval"
---

<objective>
Ship the book-detail-side UI: the metadata edit modal + form + author-row editor, the ProvenanceBadge, the ReEnrichButton, and the conditional-polling extension to `useBookWithData`. Also wires the two new web API wrappers. Runs in Wave 3 (both server waves must be green first because the UI depends on PATCH and POST routes and the shared schema in @koinsight/common).

Purpose: lock UI-01 + UI-02 + UI-03 + UI-05.

Output: Installed `@mantine/form` + `mantine-form-zod-resolver`, new presentational components, new page-level modal, wired book detail page, and a human-verification checkpoint after the core flow is runnable.
</objective>

<execution_context>
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-manual-edit-unmatched-inbox/05-CONTEXT.md (D-01 through D-15)
@.planning/phases/05-manual-edit-unmatched-inbox/05-RESEARCH.md section  "Pattern 3" (zod4Resolver) + section  "Pattern 4" (SWR polling) + section  "Pitfall 3/4"
@.planning/phases/05-manual-edit-unmatched-inbox/05-PATTERNS.md
@.planning/phases/05-manual-edit-unmatched-inbox/05-UI-SPEC.md (LOCKED copy, colors, typography)
@.planning/phases/02-canonical-genre-vocabulary/02-CONTEXT.md (CANONICAL_GENRES source)

<interfaces>
From @koinsight/common (after Plan 01 lands):
```typescript
import { metadataPatchSchema, MetadataPatch, AuthorEdit } from '@koinsight/common/types';
// (Vite resolves TS source directly in web dev; compiled dist on server.)
```

From CANONICAL_GENRES (already in packages/common/genres; Phase 2):
```typescript
import { CANONICAL_GENRES, CanonicalGenre } from '@koinsight/common/genres';
// Use .map(g => g.name) for MultiSelect options.
```

From apps/web/src/api/api.ts (the only HTTP wrapper; DO NOT create another):
```typescript
export function fetchFromAPI<T>(endpoint: string, method?: string, body?: unknown): Promise<T>;
```

From apps/web/src/api/use-book-with-data.ts (existing):
```typescript
export function useBookWithData(id: number): SWRResponse<BookWithData>;
// EXTEND: add conditional refreshInterval based on enrichment_status.
```

From existing component patterns:
- Modal + useDisclosure: apps/web/src/pages/book-page/book-card.tsx lines 38-39, 79-100
- Confirm modal: apps/web/src/pages/book-page/book-page-manage/book-delete.tsx lines 21-34
- Toast: apps/web/src/pages/book-page/book-page-manage/book-reference-pages.tsx lines 20-31

BookWithData fields needed for form init:
- book.authors (string, denormalized cache); NOT the source for author editing
- book.book_authors (array of { author: { id, name, openlibrary_key, nationality } }); use THIS when present, else fall back to splitting book.authors
- book.genres (array of { id, name }); map to `.name` for MultiSelect initialValues
- book.publication_year, book.original_language
- book.authors_source, book.genres_source, book.publication_year_source, book.original_language_source; each drives a ProvenanceBadge

WARNING: Defensive: `book.book_authors` may be empty array when the book has never been enriched; in that case derive initial authors list from the `book.authors` denormalized string by splitting on `, ` (matching applier output) or show a single empty row.

ISO 3166-1 alpha-2 list source: add a small constant file `apps/web/src/constants/iso-3166.ts` with the top ~250 codes + country names for the nationality Select. ISO 639-1 list: `apps/web/src/constants/iso-639.ts` (~185 codes). Both are static data; do not install a library for this.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install form deps + wire API wrappers + ProvenanceBadge + ReEnrichButton + useBookWithData polling</name>
  <read_first>
    - apps/web/package.json
    - apps/web/src/api/books.ts
    - apps/web/src/api/use-book-with-data.ts
    - apps/web/src/pages/book-page/book-page-manage/book-delete.tsx (notifications + mutate pattern)
    - apps/web/src/pages/book-page/book-card.tsx (Modal pattern)
    - .planning/phases/05-manual-edit-unmatched-inbox/05-UI-SPEC.md "Toasts" + "Provenance badges" + "Buttons / CTAs" tables
  </read_first>
  <action>
    1. Install two deps into the web workspace (versions from RESEARCH):
       ```bash
       npm --workspace=web install @mantine/form@8.3.12 mantine-form-zod-resolver@1.3.0
       ```
       Verify `apps/web/package.json` lists both. Run `npm install` at repo root if needed to link.
    2. Extend `apps/web/src/api/books.ts` with two exports:
       ```typescript
       import { Book, BookWithData, EnrichmentJob } from '@koinsight/common/types';
       import { MetadataPatch } from '@koinsight/common/types';

       export async function patchBookMetadata(id: Book['id'], patch: MetadataPatch) {
         return fetchFromAPI<BookWithData>(`books/${id}/metadata`, 'PATCH', patch);
       }

       export async function reEnrichBook(id: Book['id']) {
         return fetchFromAPI<{ job: EnrichmentJob | null }>(`books/${id}/re-enrich`, 'POST', {});
       }
       ```
    3. Extend `apps/web/src/api/use-book-with-data.ts` with conditional polling per D-12:
       ```typescript
       const OPEN_STATUSES = new Set(['pending', 'running']);
       export function useBookWithData(id: number) {
         return useSWR(
           `books/${id}`,
           () => fetchFromAPI<BookWithData>(`books/${id}`),
           {
             refreshInterval: (latest) =>
               OPEN_STATUSES.has(latest?.enrichment_status ?? '') ? 2000 : 0,
             revalidateOnFocus: false,
           }
         );
       }
       ```
       Do NOT break existing callers; signature unchanged.
    4. Create `apps/web/src/components/provenance-badge/provenance-badge.tsx`:
       - Props: `{ source: 'manual' | 'openlibrary' | null | undefined; fieldName?: string }`.
       - Returns `null` when `source` is null or undefined (D-15 NULL = no badge).
       - `source === 'manual'` -> `<Badge color="yellow" variant="light" size="sm" role="status" aria-label={`${fieldName ?? 'Field'} is manual`}>manual</Badge>` (UI-SPEC locked copy).
       - `source === 'openlibrary'` -> `<Badge color="blue" variant="light" size="sm" role="status" aria-label={`${fieldName ?? 'Field'} is OpenLibrary`}>OpenLibrary</Badge>`.
       - Also create `provenance-badge.module.css` (empty or with optional `.badge { /* spacing */ }` selector); import even if empty to reserve the file for future overrides.
    5. Create `apps/web/src/components/re-enrich-button/re-enrich-button.tsx`:
       - Props: `{ bookId: number; enrichmentStatus: BookWithData['enrichment_status']; variant: 'primary' | 'row' }`.
       - `isOpen = enrichmentStatus === 'pending' || enrichmentStatus === 'running'`.
       - Render `<Tooltip label="Already running" disabled={!isOpen}><Button disabled={isOpen || isSubmitting} loading={isSubmitting} leftSection={<IconRefresh size={16} />} color={variant === 'primary' ? undefined : undefined} variant={variant === 'primary' ? 'filled' : 'default'} onClick={onClick}>Re-enrich</Button></Tooltip>`.
       - `onClick` calls `reEnrichBook(bookId)`, fires the "Re-enriching..." toast (UI-SPEC "Toasts" row 'Re-enrich kicked off'), then `mutate(\`books/\${bookId}\`)` to trigger SWR polling.
       - When the SWR-hosted parent observes a terminal transition, the TERMINAL toast fires from the parent (not the button). The button itself only emits the "Re-enriching..." kickoff toast.
       - Catch errors: fire the 'Re-enrich failure' toast with UI-SPEC copy.
       - `notifications.show({ ...color, position: 'top-center' })`; never deviate from existing app toast position.
  </action>
  <verify>
    <automated>npm --workspace=web exec tsc --noEmit -p apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@mantine/form" apps/web/package.json` -> 1.
    - `grep -c "mantine-form-zod-resolver" apps/web/package.json` -> 1.
    - `grep -c "patchBookMetadata" apps/web/src/api/books.ts` -> at least 1 (export).
    - `grep -c "reEnrichBook" apps/web/src/api/books.ts` -> at least 1.
    - `grep -c "refreshInterval" apps/web/src/api/use-book-with-data.ts` -> 1.
    - `apps/web/src/components/provenance-badge/provenance-badge.tsx` exports `ProvenanceBadge`; rendering `<ProvenanceBadge source={null} />` returns nothing (null render).
    - `apps/web/src/components/re-enrich-button/re-enrich-button.tsx` exports `ReEnrichButton`.
    - `grep -c "position: 'top-center'" apps/web/src/components/re-enrich-button/re-enrich-button.tsx` -> at least 1.
    - `grep -c "Re-enriching" apps/web/src/components/re-enrich-button/re-enrich-button.tsx` -> at least 1 (toast title; matches UI-SPEC).
    - `npm --workspace=web run build` succeeds (catches any type errors in the API/shared-schema wiring).
  </acceptance_criteria>
  <done>Deps installed, API wrappers exported, conditional polling wired, ProvenanceBadge + ReEnrichButton components exist and compile; web build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build BookMetadataForm + AuthorRowEditor + BookMetadataEditModal + wire into BookPage</name>
  <read_first>
    - apps/web/src/pages/book-page/book-card.tsx (Modal + useDisclosure pattern)
    - apps/web/src/pages/book-page/book-page-manage/book-reference-pages.tsx (form loading pattern)
    - apps/web/src/pages/book-page/book-page-manage/book-delete.tsx (openConfirmModal pattern)
    - apps/web/src/pages/book-page/book-page.tsx (where the Edit + Re-enrich buttons go)
    - .planning/phases/05-manual-edit-unmatched-inbox/05-UI-SPEC.md (all locked copy)
    - .planning/phases/05-manual-edit-unmatched-inbox/05-RESEARCH.md section  "Pattern 3" (zod4Resolver skeleton)
    - packages/common/genres/index.ts (CANONICAL_GENRES export)
    - Output of Task 1 (ProvenanceBadge, ReEnrichButton, API wrappers)
  </read_first>
  <action>
    1. Create `apps/web/src/constants/iso-3166.ts` exporting `ISO_3166_ALPHA_2: Array<{ code: string; name: string }>` with ~249 entries. Keep sorted by name. Export a helper `iso3166Options()` returning `Array<{ value: string; label: string }>` for Mantine Select. Use the ISO 3166-1 alpha-2 official list (e.g., US/United States of America, GB/United Kingdom, etc.). Verify with `ISO_3166_ALPHA_2.find(c => c.code === 'US')?.name === 'United States'` style sanity check.
    2. Create `apps/web/src/constants/iso-639.ts` exporting `ISO_639_1` in the same shape (~185 entries). Helper `iso639Options()`.
    3. Create `apps/web/src/pages/book-page/author-row-editor.tsx`:
       - Props: `{ value: AuthorEdit; onChange: (next: AuthorEdit) => void; onRemove: () => void; onMoveUp?: () => void; onMoveDown?: () => void; index: number; }`.
       - Layout (Mantine `Group gap="sm"` + nested `Stack`): `TextInput placeholder="Author name" aria-label={...} value={value.name}` / `Select data={iso3166Options()} placeholder="Nationality" value={value.nationality ?? null} clearable` / read-only OL key display: `<Text c="dimmed" size="xs">{value.openlibrary_key ? value.openlibrary_key : 'No OpenLibrary link'}</Text>` / if `value.openlibrary_key`: `<ActionIcon variant="subtle" aria-label="Unlink OpenLibrary key" onClick={() => onChange({ ...value, openlibrary_key: null })}><IconX size={16} /></ActionIcon>` with `<Tooltip label="Unlink to allow re-resolution on next enrichment">` wrapper / move-up/move-down `ActionIcon`s (disabled at first/last; parent passes undefined to disable) / remove `ActionIcon color="red" aria-label="Remove author"`.
       - On remove: if `value.openlibrary_key` is present, trigger `modals.openConfirmModal({ title: 'Remove author?', children: <Text size="sm">This author has an OpenLibrary link. Removing them will not delete the author record but will detach them from this book.</Text>, labels: { confirm: 'Remove', cancel: 'Keep' }, confirmProps: { color: 'red' }, onConfirm: onRemove })`. If no OL key: call `onRemove` immediately (inline).
    4. Create `apps/web/src/pages/book-page/book-metadata-form.tsx`:
       ```typescript
       import { useForm } from '@mantine/form';
       import { zod4Resolver } from 'mantine-form-zod-resolver';
       import { metadataPatchSchema, MetadataPatch, BookWithData } from '@koinsight/common/types';
       import { CANONICAL_GENRES } from '@koinsight/common/genres';
       // ...
       ```
       - Props: `{ book: BookWithData; onSubmit: (patch: MetadataPatch) => Promise<void>; onCancel: () => void; submitting: boolean; }`.
       - Derive initial values from `book`: `authors` from `book.book_authors` (map each `ba` to `{ name: ba.author.name, nationality: ba.author.nationality ?? null, openlibrary_key: ba.author.openlibrary_key ?? null }`); if `book.book_authors` is empty/undefined, fall back to a single empty row `[{ name: '', nationality: null, openlibrary_key: null }]`. `genres`: `book.genres?.map(g => g.name) ?? []`. `publication_year: book.publication_year ?? null`. `original_language: book.original_language ?? null`.
       - `form = useForm<MetadataPatch>({ mode: 'controlled', initialValues: {...}, validate: zod4Resolver(metadataPatchSchema) });`
       - Form JSX (use Mantine `Stack gap="md"`):
         - Read-only title: `<TextInput label="Title" value={book.title} disabled />` (no provenance badge for title).
         - Authors section: label with `<Group gap="xs"><Text fw={500}>Authors</Text><ProvenanceBadge source={book.authors_source} fieldName="Authors" /></Group>`. Then the list of `<AuthorRowEditor />` components driven by `form.values.authors`, with `+ Add author` Button below calling `form.setFieldValue('authors', [...current, { name: '', nationality: null, openlibrary_key: null }])`. Move-up/down update positions via `form.reorderListItem` equivalent (or manual array splice).
         - Genres: `<MultiSelect label={<Group gap="xs"><Text fw={500}>Genres</Text><ProvenanceBadge source={book.genres_source} fieldName="Genres" /></Group>} data={CANONICAL_GENRES.map(g => g.name)} searchable clearable {...form.getInputProps('genres')} />` (label accepts a React node in Mantine 8).
         - Publication year: `<NumberInput label={<Group gap="xs"><Text fw={500}>Publication year</Text><ProvenanceBadge source={book.publication_year_source} fieldName="Publication year" /></Group>} min={1000} max={2100} allowNegative={false} {...form.getInputProps('publication_year')} />`.
         - Original language: `<Select label={<Group gap="xs"><Text fw={500}>Original language</Text><ProvenanceBadge source={book.original_language_source} fieldName="Original language" /></Group>} data={iso639Options()} searchable clearable {...form.getInputProps('original_language')} />`.
         - Footer: `<Group justify="flex-end" gap="sm"><Button variant="default" onClick={onCancel}>Cancel</Button><Button type="submit" loading={submitting}>Save changes</Button></Group>` (UI-SPEC locked copy).
       - `onSubmit` handler via `form.onSubmit(async (values) => { await onSubmit(values); })`. Strip `authors: []` by transforming empty trimmed names; if `authors` is present but contains a row with empty name, let Zod reject (already does via schema).
    5. Create `apps/web/src/pages/book-page/book-metadata-edit-modal.tsx`:
       - Props: `{ book: BookWithData; opened: boolean; onClose: () => void }`.
       - Renders `<Modal opened={opened} onClose={onClose} title="Edit metadata" size="lg" centered>` with `<BookMetadataForm book={book} submitting={submitting} onCancel={onClose} onSubmit={handleSubmit} />`.
       - `handleSubmit(patch)`: set submitting=true, try: `const updated = await patchBookMetadata(book.id, patch); await mutate(\`books/\${book.id}\`); notifications.show(UI-SPEC 'Save success' row); onClose();` catch HTTP 400: `notifications.show('Could not save changes', err.message ?? 'Some fields are invalid...')` red; other errors: 'Server error. Try again in a moment.' red. Use `position: 'top-center'` always.
    6. Wire into `apps/web/src/pages/book-page/book-page.tsx`:
       - Add `const [editOpened, { open: openEdit, close: closeEdit }] = useDisclosure(false);` at the top.
       - Add a header button group near the existing book header: `<Button leftSection={<IconEdit size={16} />} onClick={openEdit}>Edit metadata</Button>` and `<ReEnrichButton bookId={book.id} enrichmentStatus={book.enrichment_status} variant="primary" />`. Place these alongside `BookCard` in the existing Group (or just below it; planner's discretion, but keep them visible above the tabs).
       - Render `<BookMetadataEditModal book={book} opened={editOpened} onClose={closeEdit} />` near the end of the component tree.
       - Add terminal-toast effect in the page: `useEffect` watching `book?.enrichment_status`; when it transitions from `pending/running` -> `enriched` fire the 'Enrichment complete' toast; -> `failed` fire the 'Enrichment failed' toast. Track previous status with a ref.
    7. Build: `npm --workspace=web run build`. Fix any TypeScript errors.
  </action>
  <verify>
    <automated>npm --workspace=web run build</automated>
  </verify>
  <acceptance_criteria>
    - All five new component files exist and compile.
    - `grep -c "zod4Resolver(metadataPatchSchema)" apps/web/src/pages/book-page/book-metadata-form.tsx` -> exactly 1.
    - `grep -c "ProvenanceBadge" apps/web/src/pages/book-page/book-metadata-form.tsx` -> at least 4 (one per editable field: authors, genres, publication_year, original_language).
    - `grep -c "CANONICAL_GENRES" apps/web/src/pages/book-page/book-metadata-form.tsx` -> 1.
    - `grep -c "Save changes" apps/web/src/pages/book-page/book-metadata-form.tsx` -> 1 (UI-SPEC locked copy).
    - `grep -c "Edit metadata" apps/web/src/pages/book-page/book-page.tsx` -> at least 1 (trigger button label).
    - `grep -c "openConfirmModal" apps/web/src/pages/book-page/author-row-editor.tsx` -> 1 (remove-with-OL-key confirm, UI-SPEC contract).
    - `grep -c "Remove author?" apps/web/src/pages/book-page/author-row-editor.tsx` -> 1 (UI-SPEC locked copy).
    - `grep -c "aria-label=\"Unlink OpenLibrary key\"" apps/web/src/pages/book-page/author-row-editor.tsx` -> 1.
    - `apps/web/src/constants/iso-3166.ts` exports at least 200 entries.
    - `apps/web/src/constants/iso-639.ts` exports at least 180 entries.
    - `npm --workspace=web run build` passes without TS errors.
  </acceptance_criteria>
  <done>Edit modal + form + row editor implemented with locked UI-SPEC copy, wired into BookPage with Edit + Re-enrich buttons, terminal-toast effect in place, web build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 3: Human verify the edit flow end-to-end on book detail</name>
  <what-built>
    - Edit metadata modal opens from the book detail page with a @mantine/form form.
    - Provenance badges render per field label with correct colors (manual=yellow, OpenLibrary=blue, null=no badge).
    - Authors row editor lets the user add/remove/reorder rows; remove-with-OL-key fires a confirmation modal.
    - Save calls PATCH /api/books/:bookId/metadata; success toast fires; the page reflects the new data.
    - Cancel closes silently with no confirm prompt.
    - Re-enrich button calls POST /re-enrich; shows "Re-enriching..." toast; disables while pending/running; polls every 2s; fires terminal toast on enriched/failed.
    - Field-level Zod errors render inline; the form does NOT toast on per-field validation failures.
  </what-built>
  <how-to-verify>
    1. Start dev servers: `npm run dev` (root). Wait until both server and web are up.
    2. Pick any book from `/books/` -> click into its detail page.
    3. Click "Edit metadata". The modal must open within 200ms, title "Edit metadata", size ~620px (Mantine `size="lg"`).
    4. Verify each field label has a Badge (yellow "manual" or blue "OpenLibrary") IF `*_source` is set for that field; NO badge when `*_source` is null. Seed test data beforehand if needed: `sqlite3 data/dev.db "UPDATE book SET authors_source='openlibrary', genres_source='manual' WHERE id=<id>;"`.
    5. Edit Publication year to 1953; hit Save. Expect green toast "Metadata saved" with body "Manual edits will not be overwritten by future enrichment." Modal closes. Re-open modal: year = 1953 AND Publication year badge is now yellow "manual".
    6. Click "Add author", enter a name, move it up/down, remove it. Remove a row that HAS an OL key (find one in seed data or force one: `sqlite3 data/dev.db "UPDATE author SET openlibrary_key='OL/authors/TESTAUTHOR' WHERE name='<existing name>';"`). The confirm modal must open with title "Remove author?", labels "Remove" / "Keep".
    7. Enter an invalid year (e.g., 999). Inline Zod error "Year must be between 1000 and 2100" must render below the NumberInput. No red toast.
    8. Close modal via Cancel button while dirty. Modal closes. NO confirm modal. Re-open; values are those from the server (not the stale dirty state).
    9. Click Re-enrich in the page header. Blue toast "Re-enriching..." appears. Button becomes disabled with tooltip "Already running" while status is pending/running. When the worker completes (watch server logs), terminal toast fires (green "Enrichment complete" or red "Enrichment failed").
    10. Open DevTools network tab: confirm NO per-row SWR polling (only one GET `/books/:id` every 2s while status is open, matching D-12; 0 when status is terminal per Pitfall 4).
  </how-to-verify>
  <resume-signal>Type "approved" after verifying all 10 steps pass, or describe issues encountered for course correction.</resume-signal>
</task>

</tasks>

<notes>
## Verification Floor (Web UI Tasks)

This plan's React component tasks (Task 1 + Task 2) verify only via `tsc --noEmit` / `npm --workspace=web run build` plus the human-verify Checkpoint (Task 3). Automated component tests are intentionally deferred because:

- The web workspace (`apps/web`) has no React Testing Library / Vitest browser infra in `package.json` (verified in 05-RESEARCH.md Validation Architecture). There is no `test` script on the web workspace.
- Introducing RTL + jsdom + Vitest config for the web workspace is net-new scope beyond Phase 5's UI-01/02/03/05 requirements and would balloon the phase budget.
- The human-verify Checkpoint (Task 3) is the verification floor for UI behavior: modal open, provenance badge rendering, author row reorder/remove confirmation, Save -> PATCH -> SWR mutate, conditional polling cadence, and Re-enrich terminal toast are all directly observable in the browser + DevTools.
- Server-side data contracts that the UI consumes (PATCH /metadata, POST /re-enrich) ARE covered by automated supertest + integration tests in Plans 01 and 02, so the boundary the UI talks to is regression-tested.

If a future phase introduces RTL infrastructure, retrofit component tests for ProvenanceBadge, ReEnrichButton, BookMetadataForm, and AuthorRowEditor at that point.
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Express | Already secured at the server boundary in Plans 01 + 02; this plan is a consumer of the already-secured endpoints. |
| Browser (user input) -> Form state | Client-side Zod validation via `zod4Resolver` provides UX; server-side Zod is the authoritative enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering (XSS) | Rendering author name / book title in form / toasts | mitigate | React escapes all string interpolations by default. No `dangerouslySetInnerHTML` introduced. Verified by grep (see acceptance). |
| T-05-17 | Spoofing | Client-side Zod resolver disagreeing with server schema | mitigate | Both consume `metadataPatchSchema` from `@koinsight/common` (single source of truth per D-03). |
| T-05-18 | Information Disclosure | Zod error field paths leaked to UI | accept | The same schema defines the field paths; they are not sensitive (publication_year, authors, etc.). |
| T-05-19 | DoS | User pasting 10k authors into the form | mitigate | `metadataPatchSchema.authors.max(50)` enforces on the server; client form will also reject at submit. Plus Add author UI only adds one row per click (no paste vector). |

**ASVS L1 coverage:** V5 Input Validation (shared schema). V2/V3/V4/V6 N/A.
</threat_model>

<verification>
- `npm --workspace=web run build` passes.
- Manual checkpoint (Task 3) covers end-to-end flow.
- `grep -r "dangerouslySetInnerHTML" apps/web/src/pages/book-page/ apps/web/src/components/provenance-badge apps/web/src/components/re-enrich-button` -> empty (T-05-16).
</verification>

<success_criteria>
- SC-3 (roadmap): From book detail, user opens the edit modal, sees provenance badges, edits authors/genres/year/language, saves, and the page reflects the change. **[Verified by checkpoint Task 3.]**
- UI-05: Book detail shows enrichment status (implicit in the polling hook + button state) and a manual "Re-enrich" button that POSTs to /re-enrich. **[Verified by checkpoint Task 3 step 9.]**
</success_criteria>

<output>
After completion, create `.planning/phases/05-manual-edit-unmatched-inbox/05-04-SUMMARY.md`
</output>
