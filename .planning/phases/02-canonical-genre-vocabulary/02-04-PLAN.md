---
phase: 02-canonical-genre-vocabulary
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/server/src/db/seeds/06_genres.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "apps/server/src/db/seeds/06_genres.ts no longer owns a local GENRES array; it imports CANONICAL_GENRES from @koinsight/common/genres (D-19)"
    - "BOOK_GENRE_MAPPING is typed as Record<string, CanonicalGenre[]> so TypeScript catches any mapping value that falls out of the canonical list at compile time"
    - "`npm run seed` against a fresh dev DB completes without error, seeds every CANONICAL_GENRES entry into `genre`, and preserves existing BOOK_GENRE_MAPPING associations"
  artifacts:
    - path: "apps/server/src/db/seeds/06_genres.ts"
      provides: "Dev seed that sources the genre list from @koinsight/common/genres"
      contains: "CANONICAL_GENRES"
  key_links:
    - from: "apps/server/src/db/seeds/06_genres.ts"
      to: "packages/common/genres/canonical.ts"
      via: "import { CANONICAL_GENRES, type CanonicalGenre } from '@koinsight/common/genres'"
      pattern: "from '@koinsight/common/genres'"
---

<objective>
Refactor the dev seed `apps/server/src/db/seeds/06_genres.ts` so it imports `CANONICAL_GENRES` from `@koinsight/common/genres` instead of owning a local 14-entry `GENRES` array. This is D-19: one source of truth. The `BOOK_GENRE_MAPPING` object that links fake seeded books to genre names is preserved, but its type is tightened to `Record<string, CanonicalGenre[]>` so future edits to the canonical list automatically invalidate any orphaned mapping entries at compile time (RESEARCH Open Question 3).

Purpose: Prevent the dev seed from drifting from the production seed migration. Today the dev seed has 14 entries; production has 0; after this refactor both run off the same tuple.
Output: Surgical edits to one file. No migration, no new module. Same public behavior (dev seed still clears + reseeds + associates books); only the list source changes.
</objective>

<execution_context>
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-canonical-genre-vocabulary/02-CONTEXT.md
@.planning/phases/02-canonical-genre-vocabulary/02-RESEARCH.md
@.planning/phases/02-canonical-genre-vocabulary/02-PATTERNS.md
@.planning/phases/02-canonical-genre-vocabulary/02-01-SUMMARY.md
@CLAUDE.md
@apps/server/src/db/seeds/06_genres.ts
@apps/server/src/db/seeds/02_books.ts
@apps/server/src/db/factories/genre-factory.ts
@packages/common/genres/canonical.ts

<interfaces>
Current file shape (14 hardcoded entries in `GENRES`, 10 books in `BOOK_GENRE_MAPPING`):

```typescript
const GENRES = ['Fantasy', 'Science Fiction', /* ... 14 entries */];
const BOOK_GENRE_MAPPING: { [key: string]: string[] } = {
  Mistborn: ['Fantasy', 'Epic Fantasy', 'Magic', 'Adventure'],
  // ... 10 books
};
```

Target shape:

```typescript
import { CANONICAL_GENRES, type CanonicalGenre } from '@koinsight/common/genres';
const BOOK_GENRE_MAPPING: Record<string, CanonicalGenre[]> = {
  Mistborn: ['Fantasy', 'Epic Fantasy', 'Magic', 'Adventure'],
  // ...
};
```

Every value in `BOOK_GENRE_MAPPING` MUST be a member of `CANONICAL_GENRES` or compilation fails. The existing mapping references: Fantasy, Epic Fantasy, Magic, Adventure, Sword and Sorcery, Science Fiction, Space Opera, Hard Science Fiction, Military Fiction. Executor MUST verify each of these exists in `CANONICAL_GENRES` as shipped by Plan 02-01. If any is absent (e.g., if the final canonical list dropped "Military Fiction"), the executor MUST either (a) add it to canonical.ts, or (b) replace the mapping entry with a present canonical (e.g., swap "Military Fiction" for "War Fiction") and note the swap in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor 06_genres.ts to consume CANONICAL_GENRES</name>
  <read_first>
    - apps/server/src/db/seeds/06_genres.ts (full current file, 78 lines; understand the `SEEDED_GENRES` export, the `BOOK_GENRE_MAPPING` object, the `SEEDED_BOOKS.forEach` association loop, and the `console.log` tail)
    - packages/common/genres/canonical.ts (the shipped tuple; verify presence of every genre name used in BOOK_GENRE_MAPPING)
    - apps/server/src/db/factories/genre-factory.ts (confirm `createGenre(db, { name })` signature; the refactor keeps this call)
    - apps/server/src/db/seeds/02_books.ts (confirm SEEDED_BOOKS export shape)
    - .planning/phases/02-canonical-genre-vocabulary/02-CONTEXT.md §decisions D-19
    - .planning/phases/02-canonical-genre-vocabulary/02-RESEARCH.md §"Open Question 2 and 3"
    - CLAUDE.md (prettier, ASCII)
  </read_first>
  <files>apps/server/src/db/seeds/06_genres.ts</files>
  <action>
    Surgical edit. Keep the existing structure. The diff relative to the current file:

    (a) Add to the top import block (keep existing imports intact):

    ```typescript
    import { CANONICAL_GENRES, type CanonicalGenre } from '@koinsight/common/genres';
    ```

    Keep `import { Genre } from '@koinsight/common/types';`, `import { Knex } from 'knex';`, `import { db } from '../../knex';`, `import { createGenre } from '../factories/genre-factory';`, `import { SEEDED_BOOKS } from './02_books';`.

    (b) DELETE the local `GENRES` array (lines 7-23 in the current file). Replace with a comment noting the new source:

    ```typescript
    // Source of truth: @koinsight/common/genres#CANONICAL_GENRES (D-19).
    // The dev seed no longer owns a genre list; it reuses the shipped canonical tuple.
    ```

    (c) Tighten `BOOK_GENRE_MAPPING` type. Change:

    ```typescript
    const BOOK_GENRE_MAPPING: { [key: string]: string[] } = {
    ```

    to:

    ```typescript
    const BOOK_GENRE_MAPPING: Record<string, CanonicalGenre[]> = {
    ```

    Keep every mapping entry as-is. If TypeScript compilation fails for any value (meaning that value is not in the shipped canonical list), resolve each failure by EITHER adding the missing genre to `packages/common/genres/canonical.ts` (if editorially justified and the canonical length stays within [60, 80]) OR swapping the value to a present canonical name. Record every swap in the SUMMARY.

    (d) In the `seed(knex)` function body, change:

    ```typescript
    const genres = await Promise.all(
      GENRES.map((name) => createGenre(db, { name }))
    );
    ```

    to:

    ```typescript
    const genres = await Promise.all(
      CANONICAL_GENRES.map((name) => createGenre(db, { name }))
    );
    ```

    (e) Update the trailing `console.log` line from `console.log(\`✓ Seeded ${SEEDED_GENRES.length} genres with book associations\`);` - keep the character literals exactly as they are (the existing file uses the check-mark unicode `✓`; DO NOT change the check-mark, it's already present and not subject to the em-dash rule. If prettier or eslint reformats this line, leave that formatting alone). Just confirm the count interpolation still reflects the new (larger) number.

    (f) Run:

    ```
    npm --workspace=@koinsight/common run build
    npm --workspace=server run build 2>&1 | tail -20
    npx prettier --write apps/server/src/db/seeds/06_genres.ts
    ```

    The server build (which TypeScript-compiles the seed files) MUST exit 0. If it fails with an error like `"Military Fiction" is not assignable to type 'CanonicalGenre'`, apply the resolution rule from step (c).

    (g) Smoke-test `npm run seed` against a throwaway dev DB. Knex CLI has no `--dry-run`, so the executor copies the dev DB aside first:

    ```
    # Backup current dev DB if present, then reseed
    cp data/dev.db data/dev.db.bak 2>/dev/null || true
    npm run seed 2>&1 | tail -10
    # Inspect
    sqlite3 data/dev.db 'SELECT COUNT(*) FROM genre; SELECT COUNT(*) FROM book_genre;'
    # Restore
    mv data/dev.db.bak data/dev.db 2>/dev/null || true
    ```

    Expected: `SELECT COUNT(*) FROM genre` returns a number in `[60, 80]` matching `CANONICAL_GENRES.length`; `SELECT COUNT(*) FROM book_genre` returns a non-zero number reflecting the BOOK_GENRE_MAPPING associations.
  </action>
  <verify>
    <automated>cd /Users/gbumanzordev/Dev/Personal/KoInsight && npm --workspace=@koinsight/common run build 2>&1 | tail -3 && npm --workspace=server run build 2>&1 | tail -10 && npx prettier --check apps/server/src/db/seeds/06_genres.ts && ! grep -qE "^const GENRES = \\[" apps/server/src/db/seeds/06_genres.ts && grep -q "CANONICAL_GENRES" apps/server/src/db/seeds/06_genres.ts && grep -q "Record<string, CanonicalGenre\\[\\]>" apps/server/src/db/seeds/06_genres.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/server/src/db/seeds/06_genres.ts` no longer contains a local `const GENRES = [` declaration.
    - File imports `CANONICAL_GENRES` and `CanonicalGenre` from `@koinsight/common/genres`.
    - `BOOK_GENRE_MAPPING` is typed as `Record<string, CanonicalGenre[]>`.
    - The server workspace builds cleanly (`npm --workspace=server run build` exits 0) - this is the strongest signal that every `BOOK_GENRE_MAPPING` value is a valid `CanonicalGenre`.
    - prettier --check on the file exits 0.
    - (Manual smoke) `npm run seed` on a throwaway DB reports `Seeded N genres` where N equals `CANONICAL_GENRES.length`, and `book_genre` table has >= 1 row.
  </acceptance_criteria>
  <done>Dev seed consumes CANONICAL_GENRES; BOOK_GENRE_MAPPING is canonical-typed; server build passes; `npm run seed` produces the expected row counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Dev-seed-only code path. Runs from a trusted developer invocation against the local dev DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | Future edit to canonical.ts removes a genre referenced in BOOK_GENRE_MAPPING | mitigate | The `Record<string, CanonicalGenre[]>` type annotation makes any orphaned mapping value a compile-time error. |
| T-02-13 | Tampering | Two sources of truth (dev seed + migration) drift | mitigate | After this refactor both the seed migration (02-03) and the dev seed (this plan) import the same `CANONICAL_GENRES` tuple. Single source of truth per D-19. |
</threat_model>

<verification>
- `npm --workspace=@koinsight/common run build` exits 0.
- `npm --workspace=server run build` exits 0 (strongest compile-time check).
- `npx prettier --check apps/server/src/db/seeds/06_genres.ts` exits 0.
- `grep -c "const GENRES = \\[" apps/server/src/db/seeds/06_genres.ts` returns 0.
- `grep -c "CANONICAL_GENRES" apps/server/src/db/seeds/06_genres.ts` returns >= 1.
- Manual `npm run seed` smoke passes.
</verification>

<success_criteria>
- Dev seed is free of duplicated genre data; `@koinsight/common/genres` is the sole source.
- `BOOK_GENRE_MAPPING` type-safety guards against future canonical-list drift.
- `npm run seed` continues to produce a working dev DB with both `genre` rows and `book_genre` rows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-genre-vocabulary/02-04-SUMMARY.md` per the SUMMARY template. Include any BOOK_GENRE_MAPPING value swaps that were needed (or the note "no swaps required") plus the final seeded genre count.
</output>
