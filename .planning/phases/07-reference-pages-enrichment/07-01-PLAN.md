---
phase: 07-reference-pages-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/db/migrations/20260427120000_add_reference_pages_source_to_book.ts
  - packages/common/types/book.ts
  - apps/server/src/db/migrations/__tests__/phase-07-migration.test.ts
  - apps/server/src/enrichment/__tests__/fixtures/edition-no-pages.json
  - apps/server/src/enrichment/__tests__/fixtures/search-ender-with-edition-key.json
autonomous: true
requirements: [REFPAGES-03]
tags: [schema, provenance, migration]

must_haves:
  truths:
    - "After `npm --workspace=server run knex migrate:latest`, the `book` table has a `reference_pages_source` column with CHECK domain `{openlibrary, manual}` and NULL allowed."
    - "Existing rows with non-null `reference_pages` keep `reference_pages_source = NULL` after migration (no retroactive backfill, per D-02)."
    - "The shared `DbBook` type in `@koinsight/common` carries `reference_pages_source: FieldSource | null` so server and web see the new column."
    - "Wave 0 test scaffolds and fixtures exist so downstream plans can run targeted vitest commands."
  artifacts:
    - path: "apps/server/src/db/migrations/20260427120000_add_reference_pages_source_to_book.ts"
      provides: "Knex migration adding `book.reference_pages_source`"
      contains: "checkIn(['openlibrary', 'manual'])"
    - path: "packages/common/types/book.ts"
      provides: "DbBook with reference_pages_source field"
      contains: "reference_pages_source: FieldSource | null"
    - path: "apps/server/src/db/migrations/__tests__/phase-07-migration.test.ts"
      provides: "Migration column + CHECK constraint tests"
    - path: "apps/server/src/enrichment/__tests__/fixtures/edition-no-pages.json"
      provides: "Edition fixture without number_of_pages"
    - path: "apps/server/src/enrichment/__tests__/fixtures/search-ender-with-edition-key.json"
      provides: "Search fixture with cover_edition_key field"
  key_links:
    - from: "apps/server/src/enrichment/applier.ts"
      to: "book.reference_pages_source"
      via: "BookSourceRow SELECT (added in plan 03)"
      pattern: "reference_pages_source"
    - from: "packages/common/types/book.ts"
      to: "apps/server/src/books/books-router.ts"
      via: "req.book typed as Book"
      pattern: "book\\.reference_pages_source"
---

<objective>
Lay the schema and shared-type foundation for Phase 7. Adds the `reference_pages_source` column on `book` (REFPAGES-03), extends the shared `DbBook` type so both server and web compile against the new field, and creates Wave 0 test scaffolds + fixtures the rest of the phase will rely on.

Purpose: Without these foundations, plans 03 and 04 cannot read or write `reference_pages_source`, and the worker integration tests have no fixtures for the cover_edition_key + no-pages paths.

Output: One migration file, one common-type extension, one migration test, two new JSON fixtures.
</objective>

<execution_context>
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reference-pages-enrichment/07-CONTEXT.md
@.planning/phases/07-reference-pages-enrichment/07-RESEARCH.md
@.planning/phases/07-reference-pages-enrichment/07-PATTERNS.md
@CLAUDE.md

@apps/server/src/db/migrations/20260423221600_extend_book_columns.ts
@apps/server/src/db/migrations/20250412065854_add_reference_pages_to_book.ts
@packages/common/types/book.ts
@packages/common/types/author.ts
@apps/server/src/enrichment/__tests__/fixtures/edition-ender.json
@apps/server/src/enrichment/__tests__/fixtures/search-ender.json

<interfaces>
<!-- FieldSource type already exists at packages/common/types/author.ts and is imported by book.ts. -->
<!-- The four sibling *_source columns share the exact same shape; this migration is a copy-paste of one row. -->

From packages/common/types/author.ts:
```typescript
export type FieldSource = 'openlibrary' | 'manual';
```

From packages/common/types/book.ts (current DbBook):
```typescript
export type DbBook = {
  id: number;
  md5: string;
  title: string;
  authors: string;
  series: string;
  language: string;
  enrichment_status: EnrichmentStatus;
  openlibrary_work_key: string | null;
  publication_year: number | null;
  original_language: string | null;
  authors_source: FieldSource | null;
  genres_source: FieldSource | null;
  publication_year_source: FieldSource | null;
  original_language_source: FieldSource | null;
};
```

From apps/server/src/db/migrations/20260423221600_extend_book_columns.ts (the analog):
```typescript
table.string('authors_source').nullable().checkIn(['openlibrary', 'manual']);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reference_pages_source migration + common type field</name>
  <read_first>
    - apps/server/src/db/migrations/20260423221600_extend_book_columns.ts (analog migration shape — copy verbatim, drop three of four columns)
    - apps/server/src/db/migrations/20250412065854_add_reference_pages_to_book.ts (confirm `reference_pages` column already exists)
    - packages/common/types/book.ts (DbBook current shape)
    - packages/common/types/author.ts (FieldSource definition; already imported by book.ts)
  </read_first>
  <files>
    - apps/server/src/db/migrations/20260427120000_add_reference_pages_source_to_book.ts (NEW)
    - packages/common/types/book.ts (EXTEND DbBook)
  </files>
  <behavior>
    - Migration `up`: add one nullable string column `reference_pages_source` on `book` with CHECK constraint domain `['openlibrary', 'manual']`. No `defaultTo`. No backfill of existing rows (per D-02).
    - Migration `down`: drop `reference_pages_source` only. Does NOT touch `reference_pages` (that column is owned by the 2025-04-12 migration).
    - `DbBook` gains `reference_pages_source: FieldSource | null` immediately after `original_language_source` to preserve grouping.
    - After running `npm --workspace=server run knex migrate:latest`, `PRAGMA table_info('book')` lists `reference_pages_source` with `notnull = 0`.
    - After running migration twice (`migrate:latest` is idempotent on already-applied), no error.
  </behavior>
  <action>
    1. Create `apps/server/src/db/migrations/20260427120000_add_reference_pages_source_to_book.ts` implementing per D-01 / D-02. Filename timestamp `20260427120000` follows the YYYYMMDDHHMMSS convention used by the analog 20260423221600 migration. Body:
       ```typescript
       import type { Knex } from 'knex';

       export async function up(knex: Knex): Promise<void> {
         await knex.schema.alterTable('book', (table) => {
           // D-01: NULL = "never touched by provenance-aware write" (mirrors D-14 from Phase 1).
           // D-02: no retroactive backfill of this column. Existing rows with non-null
           // reference_pages keep reference_pages_source = NULL, which makes them
           // enrichment-writable under the universal D-20 semantics.
           table.string('reference_pages_source').nullable().checkIn(['openlibrary', 'manual']);
         });
       }

       export async function down(knex: Knex): Promise<void> {
         await knex.schema.alterTable('book', (table) => {
           table.dropColumn('reference_pages_source');
         });
       }
       ```
    2. Edit `packages/common/types/book.ts`. In `DbBook`, add `reference_pages_source: FieldSource | null;` immediately after `original_language_source: FieldSource | null;`. No new imports needed (FieldSource already imported on line 1).
    3. Run the migration locally: `npm --workspace=server run knex migrate:latest`.
    4. Run `npm --workspace=server run build:migrations` to compile the new migration into `dist/` so vitest can load it from the migration test.
    5. Plain ASCII only; no em-dashes (project rule).
  </action>
  <verify>
    <automated>npm --workspace=server run knex migrate:latest && npx tsc -b packages/common</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/db/migrations/20260427120000_add_reference_pages_source_to_book.ts` exists.
    - `grep -n "reference_pages_source" apps/server/src/db/migrations/20260427120000_add_reference_pages_source_to_book.ts` returns at least 3 lines (column add, drop, plus comment or nothing if comment was minimal — at least the `table.string` line and the `dropColumn` line).
    - `grep -n "checkIn\\(\\['openlibrary', 'manual'\\]\\)" apps/server/src/db/migrations/20260427120000_add_reference_pages_source_to_book.ts` returns exactly 1 match.
    - `grep -n "reference_pages_source: FieldSource" packages/common/types/book.ts` returns exactly 1 match.
    - `npx tsc -b packages/common` exits 0.
    - `npm --workspace=server run knex migrate:latest` exits 0.
    - SQLite check: `sqlite3 data/dev.db "PRAGMA table_info('book');" | grep reference_pages_source` returns one row.
  </acceptance_criteria>
  <done>Column live in dev DB, common type updated, both apps compile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [BLOCKING] Write migration test + Wave 0 fixtures</name>
  <read_first>
    - apps/server/src/db/migrations/__tests__/ (list to find an existing migration test for shape; if none, mirror enrichment integration test harness)
    - apps/server/src/enrichment/__tests__/fixtures/edition-ender.json (analog edition fixture)
    - apps/server/src/enrichment/__tests__/fixtures/search-ender.json (analog search fixture)
    - apps/server/src/knex.ts (shared knex singleton used by tests)
  </read_first>
  <files>
    - apps/server/src/db/migrations/__tests__/phase-07-migration.test.ts (NEW)
    - apps/server/src/enrichment/__tests__/fixtures/edition-no-pages.json (NEW)
    - apps/server/src/enrichment/__tests__/fixtures/search-ender-with-edition-key.json (NEW)
  </files>
  <behavior>
    - Migration test asserts: after migrations run on a fresh in-memory DB, `PRAGMA table_info('book')` includes `reference_pages_source` with `notnull = 0` and `dflt_value` is NULL.
    - Migration test asserts: inserting `reference_pages_source = 'openlibrary'` succeeds; inserting `'manual'` succeeds; inserting `'device'` fails with a CHECK constraint error.
    - Migration test asserts: inserting `reference_pages_source = NULL` (or omitting it) succeeds.
    - `edition-no-pages.json` mirrors `edition-ender.json` shape but omits `number_of_pages` entirely (not zero — absent), allowing the worker null-pages path to be exercised.
    - `search-ender-with-edition-key.json` mirrors `search-ender.json` shape and adds `cover_edition_key: "/books/OL7641985M"` so worker tests can drive the `getEdition` call path.
  </behavior>
  <action>
    1. Create `apps/server/src/db/migrations/__tests__/phase-07-migration.test.ts`. Use vitest, build a temp knex instance pointing at `:memory:` (better-sqlite3 supports this), run all migrations, then run the four assertions above. Reference the existing helpers in `apps/server/src/db/migrations/__tests__/` if any exist; if not, set up the knex client inline using the same config from `apps/server/src/knexfile.ts` (override filename to `:memory:`).
    2. Create `apps/server/src/enrichment/__tests__/fixtures/edition-no-pages.json`. Copy `edition-ender.json` verbatim and delete the `number_of_pages` line. Confirm the file is still valid JSON.
    3. Create `apps/server/src/enrichment/__tests__/fixtures/search-ender-with-edition-key.json`. Copy `search-ender.json` and add `"cover_edition_key": "/books/OL7641985M"` to the first doc inside `docs[0]`. Comma placement matters; valid JSON.
    4. Run the migration test: `npm --workspace=server exec vitest run src/db/migrations/__tests__/phase-07-migration.test.ts` and confirm green.
    5. Per D-23 / Nyquist (validation strategy): this is the [BLOCKING] Wave 0 step. Other plans must not be started until this task is green.
  </action>
  <verify>
    <automated>npm --workspace=server exec vitest run src/db/migrations/__tests__/phase-07-migration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/server/src/db/migrations/__tests__/phase-07-migration.test.ts` exists.
    - File `apps/server/src/enrichment/__tests__/fixtures/edition-no-pages.json` exists, parses as JSON, and `grep -c "number_of_pages" apps/server/src/enrichment/__tests__/fixtures/edition-no-pages.json` returns `0`.
    - File `apps/server/src/enrichment/__tests__/fixtures/search-ender-with-edition-key.json` exists, parses as JSON, and `grep -n "cover_edition_key" apps/server/src/enrichment/__tests__/fixtures/search-ender-with-edition-key.json` returns one match for `/books/OL7641985M`.
    - `npm --workspace=server exec vitest run src/db/migrations/__tests__/phase-07-migration.test.ts` exits 0 with at least 4 passing test cases (column exists, openlibrary accepted, manual accepted, NULL accepted, invalid value rejected).
  </acceptance_criteria>
  <done>Migration test green, both fixtures parseable, downstream plans unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner -> sqlite | Knex DDL execution against application DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | reference_pages_source column values | mitigate | CHECK constraint domain `{openlibrary, manual}` enforced at DB layer; any other value (including 'device' or arbitrary string) is rejected by SQLite. Verified by migration test. |
| T-07-02 | Information disclosure | migration metadata | accept | No PII in column; provenance label is non-sensitive. |
</threat_model>

<verification>
- `npm --workspace=server run knex migrate:latest` runs cleanly against dev DB.
- `npm --workspace=server exec vitest run src/db/migrations/__tests__/phase-07-migration.test.ts` is green.
- `npx tsc -b packages/common` exits 0.
- `grep -rn "reference_pages_source" packages/common apps/server/src/db/migrations/20260427120000*` returns expected lines (DbBook field + migration up + migration down).
</verification>

<success_criteria>
- New migration file lands and applies cleanly with idempotent re-run.
- `DbBook.reference_pages_source` is part of the shared type.
- Migration test validates CHECK domain + NULL acceptance.
- Two new JSON fixtures exist for downstream worker tests.
</success_criteria>

<output>
After completion, create `.planning/phases/07-reference-pages-enrichment/07-01-SUMMARY.md` describing:
- Exact migration filename and timestamp.
- Confirmation that `DbBook` was updated and packages/common compiles.
- Test file path and assertions covered.
- Fixture filenames so downstream plans can `import editionNoPages from './fixtures/edition-no-pages.json'`.
</output>
