---
phase: 07-reference-pages-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/server/src/open-library/open-library-schemas.ts
  - apps/server/src/open-library/open-library-client.ts
  - apps/server/src/enrichment/matcher.ts
  - apps/server/src/enrichment/__tests__/phase-07-schema.test.ts
  - apps/server/src/open-library/__tests__/phase-07-work-editions.test.ts
autonomous: true
requirements: [REFPAGES-01, REFPAGES-02]
tags: [openlibrary, schema, zod, http-client]

must_haves:
  truths:
    - "After Zod parse, a SearchDocSchema-validated doc preserves `cover_edition_key` (today Zod silently strips it; this is the critical finding from RESEARCH.md)."
    - "`MatcherCandidate` carries `cover_edition_key?: string` so the worker can read it after `matchWork` returns."
    - "`OpenLibraryClient.getWorkEditions(workKey)` exists, hits `/works/{key}/editions.json?limit=1`, and returns `{ entries: [{key}, ...] }` validated by Zod."
    - "`searchWork` requests the `cover_edition_key` field from OL so it actually arrives in the response."
  artifacts:
    - path: "apps/server/src/open-library/open-library-schemas.ts"
      provides: "SearchDocSchema with cover_edition_key, WorkEditionsSchema"
      contains: "cover_edition_key: z.string().optional()"
    - path: "apps/server/src/open-library/open-library-client.ts"
      provides: "getWorkEditions method"
      contains: "getWorkEditions"
    - path: "apps/server/src/enrichment/matcher.ts"
      provides: "MatcherCandidate with cover_edition_key"
      contains: "cover_edition_key?: string"
    - path: "apps/server/src/enrichment/__tests__/phase-07-schema.test.ts"
      provides: "Tests proving cover_edition_key survives Zod parse"
    - path: "apps/server/src/open-library/__tests__/phase-07-work-editions.test.ts"
      provides: "Tests for getWorkEditions HTTP path + Zod parse"
  key_links:
    - from: "apps/server/src/enrichment/worker.ts"
      to: "candidate.cover_edition_key"
      via: "matchWork return propagating SearchDocSchema field"
      pattern: "candidate\\.cover_edition_key"
    - from: "apps/server/src/enrichment/backfill-reference-pages.ts"
      to: "openLibraryClient.getWorkEditions"
      via: "D-09 option b call path (added in plan 04)"
      pattern: "getWorkEditions"
---

<objective>
Fix the Zod-strip bug on `cover_edition_key` and add the `getWorkEditions` client method needed by both the worker (plan 03) and the backfill script (plan 04). Independent of plan 01: touches `open-library/*` and `enrichment/matcher.ts`, no overlap.

Purpose: RESEARCH.md flags this as the single most important blocker — without it, `candidate.cover_edition_key` is always `undefined` at runtime and REFPAGES-01 silently fails. The `getWorkEditions` method is the D-09 option b implementation locked by the orchestrator.

Output: SearchDocSchema gains one optional field, MatcherCandidate gains the same, WorkEditionsSchema is added, OpenLibraryClient gets `getWorkEditions`, two test files prove both behaviors.
</objective>

<execution_context>
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gbumanzordev/Dev/Personal/KoInsight/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-reference-pages-enrichment/07-CONTEXT.md
@.planning/phases/07-reference-pages-enrichment/07-RESEARCH.md
@.planning/phases/07-reference-pages-enrichment/07-PATTERNS.md
@CLAUDE.md

@apps/server/src/open-library/open-library-schemas.ts
@apps/server/src/open-library/open-library-client.ts
@apps/server/src/open-library/open-library-types.ts
@apps/server/src/enrichment/matcher.ts

<interfaces>
<!-- Patterns extracted directly from the existing OL client. Reuse normalizePath + typedFetch verbatim. -->

From apps/server/src/open-library/open-library-client.ts (existing getEdition shape — analog for getWorkEditions):
```typescript
async getEdition(editionKey: string): Promise<OpenLibraryEdition> {
  const path = normalizePath(editionKey, '/books/');
  return typedFetch(`${OPEN_LIBRARY_API}${path}.json`, EditionSchema, this.deps);
}
```

From apps/server/src/open-library/open-library-schemas.ts (existing SearchDocSchema — extend by 1 line):
```typescript
export const SearchDocSchema = z.object({
  key: z.string().regex(/^\\/works\\/OL[0-9]+W$/),
  title: z.string(),
  author_name: z.array(z.string()).optional(),
  author_key: z.array(z.string()).optional(),
  first_publish_year: z.number().int().optional(),
  isbn: z.array(z.string()).optional(),
  cover_i: z.number().optional(),
  // ADD: cover_edition_key: z.string().optional(),
});
```

From apps/server/src/enrichment/matcher.ts:
```typescript
export interface MatcherCandidate {
  title: string;
  author_name?: string[];
  key?: string;
  // ADD: cover_edition_key?: string;
}
```

From apps/server/src/open-library/open-library-types.ts:16:
```typescript
// `cover_edition_key?: string` is already declared on `Doc` but Zod strips it because
// SearchDocSchema does not list it. Adding the schema field is the fix.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SearchDocSchema, MatcherCandidate, searchWork fields list</name>
  <read_first>
    - apps/server/src/open-library/open-library-schemas.ts (full file)
    - apps/server/src/open-library/open-library-client.ts (lines 50-85, the searchWork method and the fields query param)
    - apps/server/src/enrichment/matcher.ts (full file — small)
    - apps/server/src/open-library/open-library-types.ts (confirm cover_edition_key already on Doc interface)
  </read_first>
  <files>
    - apps/server/src/open-library/open-library-schemas.ts (EXTEND SearchDocSchema)
    - apps/server/src/open-library/open-library-client.ts (UPDATE fields= query param in searchWork)
    - apps/server/src/enrichment/matcher.ts (EXTEND MatcherCandidate)
    - apps/server/src/enrichment/__tests__/phase-07-schema.test.ts (NEW)
  </files>
  <behavior>
    - `SearchDocSchema.parse(input)` preserves `cover_edition_key` when input contains a string.
    - `SearchDocSchema.parse(input)` returns `cover_edition_key` undefined (not error) when input omits it.
    - `searchWork` outgoing URL includes `cover_edition_key` in the `fields=` query param.
    - `MatcherCandidate` is structurally a superset of the prior interface (cover_edition_key optional, all other fields unchanged).
    - Test cases:
      1. Parse fixture from `search-ender-with-edition-key.json` → docs[0].cover_edition_key === '/books/OL7641985M'.
      2. Parse fixture from `search-ender.json` (no key) → docs[0].cover_edition_key === undefined and parse does not throw.
      3. `searchWork` integration test (or unit on URL construction) confirms `cover_edition_key` in fields list (string match in the constructed URL).
  </behavior>
  <action>
    1. Edit `apps/server/src/open-library/open-library-schemas.ts`. In `SearchDocSchema`, add `cover_edition_key: z.string().optional(),` immediately after `cover_i: z.number().optional(),`. Preserve trailing comma style of the file.
    2. Edit `apps/server/src/open-library/open-library-client.ts`. Find the `searchWork` method's `fields:` line (around line 57 per RESEARCH.md). Append `,cover_edition_key` to the comma-separated list. Final value should be `'key,title,author_name,author_key,first_publish_year,isbn,cover_i,cover_edition_key'` (verify against current file before changing).
    3. Edit `apps/server/src/enrichment/matcher.ts`. In `MatcherCandidate`, add `cover_edition_key?: string;` after `key?: string;`. No logic change to `matchWork` itself.
    4. Create `apps/server/src/enrichment/__tests__/phase-07-schema.test.ts`. Import `SearchSchema` (or `SearchDocSchema` if exposed) from `../../open-library/open-library-schemas`. Import the two fixtures from plan 01. Write the three test cases above using vitest `describe` + `it` + `expect`.
    5. Per CLAUDE.md: plain ASCII; Prettier-formatted; no em-dashes.
  </action>
  <verify>
    <automated>npm --workspace=server exec vitest run src/enrichment/__tests__/phase-07-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cover_edition_key: z.string()" apps/server/src/open-library/open-library-schemas.ts` returns exactly 1 match.
    - `grep -n "cover_edition_key" apps/server/src/open-library/open-library-client.ts` returns at least 1 match (in the fields= string).
    - `grep -n "cover_edition_key?: string" apps/server/src/enrichment/matcher.ts` returns exactly 1 match.
    - `npm --workspace=server exec vitest run src/enrichment/__tests__/phase-07-schema.test.ts` exits 0 with at least 3 passing tests.
    - `npx tsc -b apps/server` exits 0.
  </acceptance_criteria>
  <done>cover_edition_key now flows through Zod parse and into MatcherCandidate; tests prove it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add WorkEditionsSchema and OpenLibraryClient.getWorkEditions (D-09 option b)</name>
  <read_first>
    - apps/server/src/open-library/open-library-client.ts (the `getEdition` method around line 68 — exact analog)
    - apps/server/src/open-library/open-library-schemas.ts (existing schema export style)
    - apps/server/src/open-library/__tests__/ (any existing test for the client to reuse the typedFetch + vi.stubGlobal harness; if none, mirror phase-04-integration.test.ts harness from `apps/server/src/enrichment/__tests__/phase-04-integration.test.ts`)
  </read_first>
  <files>
    - apps/server/src/open-library/open-library-schemas.ts (ADD WorkEditionsSchema export)
    - apps/server/src/open-library/open-library-client.ts (ADD getWorkEditions method)
    - apps/server/src/open-library/__tests__/phase-07-work-editions.test.ts (NEW)
  </files>
  <behavior>
    - `WorkEditionsSchema` is a `z.object({ entries: z.array(z.object({ key: z.string() })).optional().default([]) })`. Unknown extra fields on entries are stripped (default Zod). The schema tolerates `entries` missing entirely (defaults to `[]`).
    - `OpenLibraryClient.getWorkEditions(workKey)` constructs URL `${OPEN_LIBRARY_API}{normalizedPath}/editions.json?limit=1` where `normalizedPath` uses the existing `normalizePath(workKey, '/works/')` helper. Returns the validated `OpenLibraryWorkEditions`.
    - Test cases:
      1. Stub fetch returning `{ entries: [{ key: '/books/OL7641985M' }] }`. `getWorkEditions('/works/OL27448W')` resolves to that object.
      2. Stub fetch returning `{}` (no entries). `getWorkEditions(...)` resolves to `{ entries: [] }` via the Zod default.
      3. Stub fetch returning `{ entries: [{ key: '/books/OLx', random_extra: 'ignored' }] }`. Result has entries[0].key === '/books/OLx' (no extra field error; Zod strips by default).
      4. URL assertion: the fetch mock receives a URL ending in `/works/OL27448W/editions.json?limit=1`.
    - Method goes through the same `typedFetch` (which already wires through `sharedHttpLimiter`), so rate limiting is preserved without explicit work in this task.
  </behavior>
  <action>
    1. Edit `apps/server/src/open-library/open-library-schemas.ts`. After `WorkSchema` (or wherever the work-related exports sit), add:
       ```typescript
       // Used by the Phase 7 backfill (D-09 option b) to resolve cover_edition_key
       // for already-enriched books from their work key.
       export const WorkEditionsSchema = z.object({
         entries: z.array(z.object({ key: z.string() })).optional().default([]),
       });
       export type OpenLibraryWorkEditions = z.infer<typeof WorkEditionsSchema>;
       ```
    2. Edit `apps/server/src/open-library/open-library-client.ts`. Update the import line that pulls from `./open-library-schemas` to also include `WorkEditionsSchema` and the type. Add a new method directly after `getEdition`:
       ```typescript
       async getWorkEditions(workKey: string): Promise<OpenLibraryWorkEditions> {
         const path = normalizePath(workKey, '/works/');
         return typedFetch(
           `${OPEN_LIBRARY_API}${path}/editions.json?limit=1`,
           WorkEditionsSchema,
           this.deps
         );
       }
       ```
       Confirm `normalizePath` is already imported in this file (it is — used by `getEdition`).
    3. Create `apps/server/src/open-library/__tests__/phase-07-work-editions.test.ts`. Mirror the `vi.stubGlobal('fetch', ...)` pattern from `apps/server/src/enrichment/__tests__/phase-04-integration.test.ts`. Build a minimal mock that captures the URL the client called and returns one of the three response shapes per the four test cases.
    4. Run the test suite: `npm --workspace=server exec vitest run src/open-library/__tests__/phase-07-work-editions.test.ts`.
    5. Per RESEARCH.md A1: the response shape `{ entries: [{key}, ...] }` is assumed but not live-verified. The Zod schema is permissive (extra fields stripped, entries optional with default), so an OL response with slightly different shape (e.g., the entries array still keyed `entries`) will still parse if the entries array is present. If OL ever returns the editions list under a different key, this test will catch it because integration with the real API would surface the missing `entries` field. No live curl required for plan 02; plan 04 will exercise this against fixtures.
  </action>
  <verify>
    <automated>npm --workspace=server exec vitest run src/open-library/__tests__/phase-07-work-editions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "WorkEditionsSchema" apps/server/src/open-library/open-library-schemas.ts` returns at least 2 matches (declaration + type export).
    - `grep -n "getWorkEditions" apps/server/src/open-library/open-library-client.ts` returns at least 1 match (method declaration).
    - `grep -n "/editions.json?limit=1" apps/server/src/open-library/open-library-client.ts` returns exactly 1 match.
    - `npm --workspace=server exec vitest run src/open-library/__tests__/phase-07-work-editions.test.ts` exits 0 with at least 4 passing tests.
    - `npx tsc -b apps/server` exits 0.
  </acceptance_criteria>
  <done>getWorkEditions wired through typedFetch + sharedHttpLimiter; Zod schema permissive enough for OL shape variation; ready for plan 04 backfill consumer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| openlibrary.org -> server | Untrusted JSON crosses; Zod validates at parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | OL response body | mitigate | `WorkEditionsSchema.parse()` rejects malformed payloads at the boundary; entries[].key required to be string. |
| T-07-04 | Information disclosure | workKey in URL | accept | Public OL identifier; not sensitive. |
| T-07-05 | Denial of service | unbounded entries array | accept | `?limit=1` request parameter caps response size; client only reads `entries[0]`. |
</threat_model>

<verification>
- Both new vitest test files green.
- `npx tsc -b apps/server` exits 0 (type system happy with new optional field on candidate).
- Manual sanity: `grep -rn "candidate.cover_edition_key" apps/server/src/` returns nothing yet (worker wires it in plan 03).
</verification>

<success_criteria>
- SearchDocSchema, MatcherCandidate carry `cover_edition_key`.
- `searchWork` requests it in the fields list.
- `WorkEditionsSchema` and `getWorkEditions` available for plan 04 to import.
- Zod-strip bug closed and proven by unit test.
</success_criteria>

<output>
After completion, create `.planning/phases/07-reference-pages-enrichment/07-02-SUMMARY.md` describing:
- Confirmation that SearchDocSchema parse on a fixture with cover_edition_key now returns the field.
- The new method signature `getWorkEditions(workKey: string): Promise<OpenLibraryWorkEditions>`.
- Any gotchas observed (e.g., OL response shape variation if encountered during testing).
- Note that plan 03 can now read `candidate.cover_edition_key` and plan 04 can call `openLibraryClient.getWorkEditions(...)`.
</output>
